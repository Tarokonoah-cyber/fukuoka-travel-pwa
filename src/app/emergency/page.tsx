import { InfoCard } from "@/components/InfoCard"; import { NoticeBox } from "@/components/NoticeBox"; import { PageHeader } from "@/components/PageHeader"; import { emergency } from "@/data/emergency";
export default function EmergencyPage(){return <div className="page-enter emergency-page"><PageHeader eyebrow="KEEP THIS CLEAR" title="緊急資訊" description="需要時，先停下來，再一步一步處理。"/><div className="emergency-phones">{emergency.phones.map(p=><a href={`tel:${p.number}`} key={p.number}><span>{p.label}</span><strong>{p.number}</strong><small>點一下撥打</small></a>)}</div><InfoCard title="住宿地址"><p><strong>{emergency.hotel}</strong></p><p className="address">{emergency.address}</p></InfoCard><NoticeBox tone="plain" title="需要協助時">先確保人身安全，請飯店櫃台或附近店員協助。重要號碼也要另外存在手機聯絡人。</NoticeBox><div className="guidance-list">{emergency.guidance.map((g,i)=><InfoCard key={g.title}><div className="guidance-title"><span>{String(i+1).padStart(2,"0")}</span><h2>{g.title}</h2></div><p>{g.body}</p></InfoCard>)}</div></div>}
