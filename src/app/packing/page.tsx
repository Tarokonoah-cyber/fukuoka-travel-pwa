import { Checklist } from "@/components/Checklist"; import { NoticeBox } from "@/components/NoticeBox"; import { PageHeader } from "@/components/PageHeader"; import { packingCategories,packingItems } from "@/data/packing"; import { STORAGE_KEYS } from "@/lib/storage";
export default function PackingPage(){return <div className="page-enter"><PageHeader eyebrow="BEFORE DEPARTURE" title="行李清單" description="勾選後會保存在這支手機。"/><NoticeBox title="先確認這個">護照、每日用藥與手機，出門前再檢查一次。</NoticeBox><Checklist items={packingItems} categories={packingCategories} storageKey={STORAGE_KEYS.packing} customPlaceholder="例如：媽媽的薄外套"/></div>}
