import { Checklist } from "@/components/Checklist"; import { NoticeBox } from "@/components/NoticeBox"; import { PageHeader } from "@/components/PageHeader"; import { wishlistCategories,wishlistItems } from "@/data/wishlist"; import { STORAGE_KEYS } from "@/lib/storage";
export default function WishlistPage(){return <div className="page-enter"><PageHeader eyebrow="IF WE HAVE TIME" title="願望清單" description="想去，但不用勉強全部排進去。"/><NoticeBox tone="blue" title="行程留白">依天氣和體力挑一個就好，休息也是旅程的一部分。</NoticeBox><Checklist items={wishlistItems} categories={wishlistCategories} storageKey={STORAGE_KEYS.wishlist} customPlaceholder="新增想去的地方"/></div>}
