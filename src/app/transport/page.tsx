import Link from "next/link"; import { InfoCard } from "@/components/InfoCard"; import { NoticeBox } from "@/components/NoticeBox"; import { PageHeader } from "@/components/PageHeader"; import { transportRoutes } from "@/data/transport";
export default function TransportPage(){return <div className="page-enter"><PageHeader eyebrow="MOVE WITH EASE" title="交通資訊" description="少轉乘、留時間，不追車。"/><NoticeBox title="媽媽同行原則">行李多、下雨或很熱時，短程計程車是保留體力的選項。</NoticeBox><Link className="map-entry-link" href="/map"><span>飯店、車站與景點</span><strong>查看旅程地圖</strong><b aria-hidden>→</b></Link><div className="transport-list">{transportRoutes.map((r,i)=><InfoCard key={r.id} className="route-card"><div className="route-top"><span>{String(i+1).padStart(2,"0")}</span><b>{r.method}</b><small>{r.duration}</small></div><div className="route-points"><strong>{r.from}</strong><i>→</i><strong>{r.to}</strong></div><dl>{r.walkingLevel&&<div><dt>步行量</dt><dd>{r.walkingLevel}</dd></div>}<div><dt>轉乘數</dt><dd>{r.transferCount}</dd></div><div><dt>主路線</dt><dd>{r.transfer}</dd></div>{r.accessibilityNote&&<div><dt>動線提醒</dt><dd>{r.accessibilityNote}</dd></div>}<div><dt>高溫備案</dt><dd>{r.heatPlan}</dd></div><div><dt>下雨備案</dt><dd>{r.rainPlan}</dd></div><div><dt>注意事項</dt><dd>{r.note}{r.mapHref&&<> <a href={r.mapHref} target="_blank" rel="noopener noreferrer">Google Maps ↗</a></>}</dd></div><div className="mom-row"><dt>媽媽友善</dt><dd>{r.momFriendlyNote}</dd></div></dl></InfoCard>)}</div></div>}
